# Database — use one of the two options below

# Option A (recommended): single URL
DATABASE_URL="mysql://user:password@host:PORT/database?ssl-mode=REQUIRED"

# Option B: separate vars (e.g. Aiven) — uncomment and set
# DB_HOST=your-db.aivencloud.com
# DB_PORT=13040
# DB_USER=avnadmin
# DB_PASS=your-password
# DB_NAME=defaultdb

# Optional: Pool size. Aiven free tier: 5 (default) or 3 for extra safety. See docs/AIVEN-FREE-TIER.md
# DB_POOL_SIZE=5

# Google Sign-In (optional – for "Sign up / Continue with Google")
# Get Client ID: https://console.cloud.google.com/ → APIs & Services → Credentials → Create OAuth 2.0 Client ID (Web application)
# Add authorized JavaScript origins: http://localhost:3000, https://your-domain.com
NEXT_PUBLIC_GOOGLE_CLIENT_ID="your-client-id.apps.googleusercontent.com"

# Forgot password + Project invite emails (get key at https://resend.com)
RESEND_API_KEY="re_xxxx"
# Sender: use your verified domain (e.g. Asanamanager <noreply@yourdomain.com>).
# onboarding@resend.dev only sends to the test email you add in Resend dashboard – for real inbox delivery, add and verify a domain in Resend → Domains.
RESEND_FROM="Asanamanager <onboarding@resend.dev>"
